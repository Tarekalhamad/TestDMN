server.port=${PORT:8080}

# Enable DMN runtime type checking
org.kie.dmn.runtime.typecheck=true

# DMN Editor Configuration
dmn.editor.storage-path=${user.dir}/dmn-storage

# SpringDoc - only scan our package (Kogito-generated controllers have broken $ref annotations)
springdoc.packages-to-scan=com.example.discount
